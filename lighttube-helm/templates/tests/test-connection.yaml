apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lighttube-helm.fullname" . }}-test-connection"
  labels:
    {{- include "lighttube-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lighttube-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
